@model List<Position>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}



<div style="margin-top:40px" class="col-12">
    <div class="bg-light rounded h-100 p-4">
       
            <h6 class="mb-4">Position Table</h6>
            <a  class="btn btn-primary">Create</a>
      
       
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Name</th>
                    <th scope="col">Status</th>
                    <th scope="col">Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach(var item in Model)
                {
                    <tr>
                        <th scope="row">@item.Id</th>
                        <td>@item.Name</td>
                       <td>
                            @if (item.IsDeactive)
                            {
                                <span class="text-danger">Passiv</span>
                            }
                            else
                            {
                                <span class="text-success">Activ</span>
                            }
                        </td>
                        <td>
                            <a class="btn btn-warning">Update</a>
                            @if (item.IsDeactive)
                            {
                                <a class="btn btn-danger">Activate</a>
                            }
                            else
                            {
                                <a class="btn btn-success">Deactivate</a>
                            }

                        </td>
                    </tr>
                }

                
            </tbody>
        </table>
    </div>
</div>